## What / why

## Checklist
- [ ] `contract/` still builds (if touched)
- [ ] Verifier flows still pass: `job-pass` → PASS, `job-fail` → PARTIAL (not chargeable)
- [ ] No secrets / funded keys in the diff
